<Window x:Class="Sports.Filtr"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sports"
        mc:Ignorable="d"
        Title="Filtr" Height="450" Width="800">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8" >
            <TextBox x:Name="tbSearch" Width="260" Background="Pink" />
            <Button Content="Обновить" Background="Pink"/>
            <Button Content="переход" Click="Register" Background="Pink"/>
        </StackPanel>
        <DataGrid x:Name="grid" Margin="8" AutoGenerateColumns="False" ItemsSource="{Binding Workout}" Background="Pink" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="2*" />
                <DataGridTextColumn Header="Дата" Binding="{Binding Datetime, StringFormat=g}" Width="*"/>
                <DataGridTextColumn Header="Длительность" Binding="{Binding Duration}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</Window>
